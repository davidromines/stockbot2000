Requirement 4 is not met: cross_validate must run the genome on the INTERSECTION of tickers both providers actually return data for, so the comparison is like-for-like. Fetch bars from each provider for the requested tickers, compute set(primary tickers with bars) & set(secondary tickers with bars), and build BOTH panels from only those tickers. In the acceptance test, CCC exists only in the primary, so securities_primary and securities_secondary must both be 2. Also report the tickers dropped from each side as only_primary_tickers / only_secondary_tickers lists. Change nothing else.