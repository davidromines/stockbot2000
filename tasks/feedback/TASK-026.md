Two assertions are wrong, the code is right. (1) test_forward_promotion: evaluate_forward() calls promote_candidates() at its end, so a lone qualifying strategy correctly ends at LIVE_CANDIDATE; assert its state is in (QUALIFIED, LIVE_CANDIDATE). (2) test_family_limit: the tests share one connection, so promote_candidates() also returns the earlier test's good_fam winner; filter cands to r['family'] == 'shared_fam' before asserting exactly one. Keep every other check unchanged.