The file is TRUNCATED. It stops mid-sentence in the "Daily pipeline" section
at 18 lines, with six of the required sections missing entirely. You ran out
of output budget — the opening paragraph is good but the document has to fit
in one response.

Rewrite it COMPACT. Same structure, far less prose:

- Opening: keep what you wrote, it is right. Three sentences maximum.
- "Daily pipeline": ONE fenced code block with the chain, then a short table
  of stage -> what it produces. No paragraph per stage.
- "Research layer": a table of module -> what it decides. One line each.
- "Data layer": a table of module -> what it owns. One line each.
- "Boundaries": three numbered items, two sentences each — what the boundary
  is, and why it exists.
- "Storage": four lines. Two databases, why separate.
- "Where to look when a number is wrong": a table of symptom -> modules. Four
  rows.

Target 90 to 130 lines total. Tables instead of prose everywhere. The
document is a map, not an essay — someone reading it wants to know where to
look, not to be persuaded of anything.

Write the WHOLE file in one response. If you find yourself running long, cut
prose, never a section.