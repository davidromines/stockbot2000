# Changelog

Generated from git by `changelog.py`. Do not edit by hand.

272 commits.

## 2026-09-24 (18 commits)

- TASK-018: move to COMPLETE (`3098fc9`)
- TASK-018: fix the local-import shadowing introduced in the correction (`42411a6`)
- TASK-018: accounting and paper-engine regression tests (DeepSeek, corrected here) (`91d3759`)
- H2: accounting.py — one liquidation-basis P&L for every fund (`9ef8ee0`)
- Paper engine: mark at the last real close, never overwrite a held position (`b1e9107`)
- H1: Phase 13 architecture map — existing owner for every spec concept (`07e5803`)
- TASK-016: move to COMPLETE (`228b2e0`)
- Describe the 36 undocumented tables; generate docs/DATA_DICTIONARY.md (Phase 6 §29) (`260d020`)
- TASK-016: data dictionary generator (DeepSeek, reviewed here) (`6f0eddf`)
- Enter Addendum B — the final build directive — and start the build (`3e683f7`)
- Enter Addendum A — the autonomous 5-slot trading system — into the plan (`5628ebc`)
- List Phase 13 in ROADMAP.md and CLAUDE.md (`1ca93b5`)
- Enter Phase 13 — Strategy Factory 2.0 — into the plan (`33c9b73`)
- Record the stop-width result: the tight-stop lead is refuted (`cfdcef9`)
- Close phase 07 with today's re-measurement, and correct stale phases (`e449bb8`)
- Document why heavy jobs must go through run_bounded.sh (`302ed7e`)
- Bound the stop sweep's exit prices to the block plus 120 days (`6b4a288`)
- Run batch jobs in their own memory-capped scope (`9397c9a`)

## 2026-09-23 (29 commits)

- Add signal_decay.py (Phase 6 section 20), with an honest sample and t (`333aac8`)
- Run the stop sweep in blocks and write each row as it lands (`bd40800`)
- Fail the freshness gate when we hold a bar newer than the market (`dc7f6f3`)
- Never store an in-progress bar as a close (`49ee04e`)
- Mark Phase 12 items 40 and 42 complete (`3d23e5b`)
- TASK-015: crypto fund and slate tests, and the defect they found (`84bf6ae`)
- Add the crypto order slate, broker adapters and crypto risk floors (item 42) (`6db07ce`)
- Add the crypto paper fund (Phase 12 item 40) (`e91fe06`)
- Correct the grid fee model, and share one engine with the paper fund (`91135f0`)
- TASK-014: pin the Value Fund's quarterly review gate (DeepSeek) (`48e9519`)
- Correct the roadmap status table (`6ea552e`)
- Finish item 30: the Value Fund now reviews quarterly and marks daily (`1fc975d`)
- Wire crypto into the daily loop and the dashboard (`92aa041`)
- Grid-DCA loses to random entry on every pair once fees are charged (`8314620`)
- Add the crypto null surface, per symbol (`ed81d55`)
- Record crypto listing status at load time (`f74a284`)
- TASK-013: crypto data regression test (DeepSeek, corrected here) (`79314d7`)
- Add crypto OHLCV in its own table, with the session convention decided (`e9b12b5`)
- Add the live status dashboard (`18cde06`)
- Remove the automation editorializing from the Phase 12 spec (`e0df303`)
- Add Phase 12: crypto fund, from the requested source article (`fa6c42c`)
- TASK-012: add ARCHITECTURE.md, and fix the ADO parser that was eating it (`f6078a7`)
- TASK-011: rewrite PROJECT_STATUS for Phases 6-11 (DeepSeek) (`904cb8d`)
- Record Stages E and F complete (`5b8e984`)
- TASK-010: tests for stop conditions and compute priority (DeepSeek) (`0fca7a0`)
- Add stop conditions, compute priority, and the daily research loop (`a5aa9e9`)
- TASK-009: regression tests for allocation and roster (DeepSeek) (`cec78e1`)
- Add capital allocation, roster management and the live pipeline (`ecbf01d`)
- Add the promotion policy: two gates, and it refuses everything today (`619c9d2`)

## 2026-09-22 (68 commits)

- TASK-007: fix the TypeError TASK-006 introduced (DeepSeek) (`06c86a8`)
- TASK-006: remove the alphabetical sampling bias from the Value Fund (DeepSeek) (`38c682c`)
- TASK-005: show the Value Fund in the daily report (DeepSeek) (`3ede550`)
- TASK-004: restrict Value Fund candidates to common stock (DeepSeek) (`df94f63`)
- TASK-003: regression test for the Value Fund (DeepSeek, reviewed here) (`6a5c4fa`)
- Add the Stockbot Value Fund, and fix the ADO path allowlist (`e4e9864`)
- Record item 29 complete (`112f46b`)
- Add the value score and watchlist, with the formula deliberately unfixed (`ffd7327`)
- Record item 28 complete (`9998383`)
- Add the intrinsic value engine, with its honesty machinery built in (`67a5528`)
- Record item 27 complete (`4d1a63a`)
- Add the industry-aware valuation engine (`bdfce5f`)
- Correct the stale "most important gap" at the top of CLAUDE.md (`bb70e50`)
- Document the statement engine and its coverage limits (`227b007`)
- Add the financial statement analysis engine (`e6c4d3d`)
- Store filing provenance at load time, and document the ticker defect (`257114d`)
- Add point-in-time provenance and industry classification, and unhide 212 large caps (`1e79ec4`)
- Document the research library and governed generation (`50503ec`)
- Record Stage C status (`a741ad9`)
- Gate generation behind five governance requirements (`ad7b554`)
- Add the research library: 35 strategies with provenance, none supported (`5f51286`)
- Document the Strategy League and what it currently reports (`41deed5`)
- Wire the league into the daily pipeline, and record Stage B status (`23a0169`)
- Add correlation measurement and the top-five-eligible rule (`3cb9008`)
- Track backtest-to-forward degradation, in comparable units (`9c65d56`)
- Add the persistent scoreboard, whose main job is refusing to rank (`839a629`)
- Migrate the 21 forward funds into the league without moving any data (`c49cbd5`)
- Add strategy identity, immutable versioning and the lifecycle (`11f6f7c`)
- Regenerate the integrity report after the fundamentals repair (`1fc2885`)
- Restart the conviction funds, and leave the eleven-day hole in their curves (`f7c6064`)
- Correct the recorded cause of the stalled conviction funds (`2872c77`)
- Catch stale derived tables, and run the seeded vs seed-free comparison (`77e4f43`)
- Record Stage A status against the implementation order (`553e418`)
- Pre-register the seeded vs seed-free search comparison (`a02db3a`)
- Report what random strategies actually achieve, beside the theoretical bar (`0b6f8be`)
- Accumulate the random control nightly, and stop it overclaiming (`c5ec65b`)
- Expand the random control into a persistent calibration distribution (`8788a86`)
- Record the sealed window as physically separated (`24f523e`)
- Carve the sealed window into its own read-only snapshot (`48fccdf`)
- Add the research integrity report (Phase 6 section 26) (`a6be991`)
- Add multiple-testing accounting (Phase 6 section 18) (`5b85f2e`)
- Add the experiment registry with pre-registration (Phase 6 sections 11 and 12) (`6f8853b`)
- Document the validation firewall (Phase 6 section 9) (`4569a21`)
- TASK-002: fundamental availability audit (DeepSeek, reviewed here) (`729546b`)
- Keep ADO state directories from vanishing (`d4f191d`)
- Add the point-in-time universe, and measure survivorship per date (`7e8e09b`)
- Add the immutable truth set (Phase 6 section 4) (`6b6c19f`)
- Add strategy ancestry and freshness gating (Phase 6 sections 2 and 8) (`ef18019`)
- Freeze continuous strategy search (Phase 6 section 1) (`153e50b`)
- Document ADO and record what the first delegated task taught (`d04a73c`)
- Add a merge command that refuses un-approved work (`19c99e7`)
- TASK-001: data completeness audit (implemented by DeepSeek, reviewed here) (`1de151c`)
- wip: ADO infrastructure before first delegated run (`d44170b`)
- Build the quality gate: test runner + regression suite for all 11 bug classes (`9843a78`)
- Record that the trial counter is append-only and never resets (`3c49980`)
- Add Phases 7-11 and integrate the whole direction into the plan (`2f98244`)
- Add Phase 6 — research integrity and independent validation — to the plan (`1ec2075`)
- Back up the data that cannot be regenerated (`c1ae26e`)
- Add consensus.py: one pre-registered hypothesis from many survivors (`ceff55b`)
- Record the seed-convergence retraction (`86ab03c`)
- Put liquidity and market cap on the quote (`5a16e5f`)
- Record the execution modules in CLAUDE.md (`24155a5`)
- Add the execution runner and PROJECT_STATUS (`d39b1b8`)
- Add the broker abstraction and execution engine (`6de926f`)
- Add six kill switches, all failing closed (`d9d4ab7`)
- Add the signal schema and the risk engine, with tests (`0dec604`)
- Add the Robinhood Agentic execution system to the plan (`69ff213`)
- Add stop_sweep.py: test the tight-stop lead directly (`3c806fd`)

## 2026-09-19 (3 commits)

- Keep the strategy search alive with a cron watchdog (`09fff8d`)
- Raise the excess gate to its recalibrated level (`a329d2a`)
- Fix add_size_buckets crashing on a single review date (`dbd86c3`)

## 2026-09-17 (12 commits)

- Stop tracking the remaining generated daily files (`a8fb777`)
- Ignore the generated Claude Fund status file (`026af4a`)
- Update README: daily.sh, the real database, and the funds (`abbd4ba`)
- Update the roadmap to match what was actually built (`c78b71d`)
- Bring CLAUDE.md up to date with the whole project (`4c4902c`)
- Open five bull/bear ETF switching funds and step them daily (`7173e22`)
- Test long/inverse switching across every pair and leverage level (`b9db8f5`)
- Search ERX/ERY momentum, name every paper fund, add the Claude Fund (`2e78dc8`)
- Ignore the generated fund report (`42bcff4`)
- Add a daily fund report and record actual fill quantities (`8780b45`)
- Prove the stop-loss sell reaches the phone (`33d8a0e`)
- Add a size floor to the Lab and classifier paths (`080a551`)

## 2026-09-15 (12 commits)

- Stop measuring staleness against the table being updated (`b098d4f`)
- Fix NameError that broke this morning's daily capture (`47cf6cd`)
- Fix off-by-one in reported holding period (`af760e5`)
- Add --compare-fills so the null's convention is measurable (`5319ccc`)
- Record the next-bar fill result: the signal is gross negative (`1745a01`)
- Add --fill so the look-ahead can be measured, not asserted (`67b7090`)
- Fill the backtest on the next bar too (`6fc2120`)
- Close out phase 07 in the roadmap (`d494c18`)
- Never fall back to close fills silently (`38914cf`)
- Measure the null open-to-open, matching the simulator (`7f77e8f`)
- Fill orders at the next open, and let stops gap (`88b1a1c`)
- Expose opening prices to the simulator (`8166730`)

## 2026-09-14 (12 commits)

- Record actual fills, and stop reporting fake P&L before the first bar (`8874d91`)
- Stop tracking the generated order slate (`4b5e035`)
- Record the re-measured walk-forward under size neutralisation (`7dc1c7b`)
- Re-measure the screen records under size neutralisation (`dd1cbb2`)
- Record the size and liquidity findings in CLAUDE.md (`ccfc9a0`)
- Rank fundamental screens within size buckets (`f8b9882`)
- Apply the liquidity floor to the fundamental screens (`07814dc`)
- Drop "market, regular hours" from the slate (`3c2a4ed`)
- Format the Telegram slate for a phone screen (`9d5d87c`)
- Log Telegram token rotation as a security task (`7145aa7`)
- Add notify.py and wire it into the daily job (`811a1f3`)
- Add orders.py, resize to $20 x 5, and fix two circularity bugs (`40b2d76`)

## 2026-09-13 (14 commits)

- Add the daily book, PEAD measurement, and two data-quality fixes (`70a2757`)
- Add edgar_events.py: what actually happens after an 8-K (`c3e121f`)
- Implement Buffett's method: QMJ quality, betting against beta, no leverage (`881e935`)
- Add monitor.py: live dashboard, terminal and web, zero dependencies (`78bdc72`)
- Walk-forward the conviction screens. None reliably beats SPY. (`e3922cb`)
- Verify the daily capture end to end (`1bb4a9e`)
- Paper-trade the conviction books; stop a stale model taking down every run (`a670b4c`)
- Add conviction.py: the low-turnover fundamental book (`8388ad6`)
- Implement the CHS distress model, transcribed from the paper (`e437ca1`)
- Add value_metrics.py: 36 constructed fundamental indicators (`64074da`)
- Ingest SEC XBRL fundamentals: 400,700 filings, 19.0M facts (`54da3a9`)
- Add four range and liquidity indicators; fix the features migration (`ecb5ca1`)
- Add lab_loop.sh: continuous search until stopped (`39b23d9`)
- Record the corrected run: 3 of 56 survive, one worth forward-testing (`8035d6d`)

## 2026-09-12 (38 commits)

- Record the exit-pricing bug in CLAUDE.md (`78de7e1`)
- Price exits from the unfiltered series. 71% of reported profit was a bug. (`05df0c1`)
- Record why the ensemble re-test barely dented the crash-buyers (`ccf4848`)
- Add the synthetic delisting ensemble, its validator, and the re-test harness (`eeb9ec0`)
- Review pass against the enumerated delisting data; fix two entry points (`45e8b92`)
- Add external data sources: SEC EDGAR registry and the delisting registry (`b4e31f7`)
- Record the database corruption, its cause and the recovery (`0bffd90`)
- Stop versioning SQLite databases and their write-ahead logs (`ecff66f`)
- Add stress_test.py: bound how much a result depends on the missing companies (`aa26ddc`)
- Count paper-trading holding periods in trading days, not invocations (`0b0db4f`)
- Correct the data-freshness claim in CLAUDE.md (`0d8a38c`)
- Fix the daily top-up, which was silently fetching nothing (`96da27b`)
- Cap search capacity: the constraint is data, not compute (`f5e9ffd`)
- Refuse to seal strategies this data cannot evaluate (`7c19317`)
- Reject rules whose answer is fixed before any data arrives (`cb45ce1`)
- Let paper trading run Lab genomes, not just the classifier (`00933bb`)
- Add structural deduplication and fitness sharing (`f415dd4`)
- Close the ERX/ERY paired-ETF experiment: FAIL (`fb324c3`)
- Update the tracker: price-filter finding and the seed baseline (`82d0acd`)
- Pass worker state as one dict so pool init cannot go out of arity (`4cd5154`)
- Record the published-strategy baseline (`9bdac1a`)
- Add seeds.py: 20 published strategies as genomes (`357a21a`)
- Add an effect-size term to fitness (`955f0b0`)
- Record the price-filter result in CLAUDE.md (`9e81c05`)
- Raise the excess gate to the recalibrated 95th percentile of noise (`0d3d011`)
- Add a void decision for verdicts reached under a wrong benchmark (`7a638e0`)
- Extrapolate below the cheapest band; measure finer under $20 (`9ef696c`)
- Carry the benchmark through a scored-zero result (`027847e`)
- Interpolate the null across price instead of stepping at band edges (`5e29195`)
- Store excess over the null, and rank the shortlist by it (`4c5a6c2`)
- Restore the Strategy Lab comment above its own block (`6a93f2f`)
- Bucket the null by entry price, not just holding period (`21a728a`)
- Specify the ERX/ERY experiment with pass/fail criteria set in advance (`d0c8d01`)
- Add seed-strategy and paired-ETF items to the backlog (`8dd2af3`)
- Recalibrate gates against the hold-scaled null (`1d5ae2d`)
- Add the project tracker as a repo file (`fa3b0fa`)
- Scale the null to each strategy's actual holding period (`3822b9b`)
- Cache the evaluation panel per window in the promotion ladder (`d29c8bc`)

## 2026-09-11 (40 commits)

- Recalibrate the excess gate against corrected noise levels (`ce8f725`)
- Fix variable shadowing that silently zeroed every excess P&L (`876f58f`)
- Evaluate candidates in parallel (`6f316ab`)
- Complete the Internet Archive reconstruction: the gap is twice what we thought (`de3bb8e`)
- Pace the Internet Archive pull so it finishes instead of failing (`9a55484`)
- Fix the deflated Sharpe: two bugs, not one (`fed6328`)
- Calibrate the promotion gates against a random-strategy control (`20a5170`)
- Score fitness on excess over the null, not on raw P&L (`d2a0233`)
- Measure the null: what doing nothing clever actually earns (`3f2d3a2`)
- Phase 10: lab dashboard (`0add7bc`)
- Phase 10: promotion ladder with a mechanically sealed holdout (`969faba`)
- Add the paper trading toolset (`06ac00d`)
- Add the experiment leaderboard, ranked by money (`6bcb08e`)
- Phase 09: evolutionary search loop (`2af02ce`)
- Phase 09: idea ledger (`b1e592e`)
- Phase 08: genome, simulator and reward for open-ended rule synthesis (`552aee4`)
- Widen the Strategy Lab from parameter tuning to open-ended rule synthesis (`a78cd38`)
- Phase 07: model trading costs, and report P&L as the primary number (`7079fb8`)
- Record the walk-forward result in the briefing (`c7895ad`)
- Add walk-forward validation with stored out-of-sample predictions (`6a890e7`)
- Apply the measured survivorship haircut to every backtest (`75c86b0`)
- Record the measured survivorship bias and the price-anomaly guard (`27a472c`)
- Handle archive.org rate limiting instead of grinding through it (`e5051cd`)
- Measure survivorship bias in percent per year against CRSP (`e3b377b`)
- Flag tickers whose adjusted prices are physically impossible (`1a14896`)
- Measure the survivorship gap from Internet Archive snapshots (`5fb38a0`)
- Replace the unreachable score>=70 rule in the trading mandate (`4555e98`)
- Add price and liquidity floors; stop the strategy buying penny stocks (`5395eb4`)
- Select the top N by score each day instead of a fixed cutoff (`5ef2b41`)
- Fix the OOM: chunked reads, SQL labelling, and a row cap (`dc35d69`)
- Migrate train_model, score and backtest onto market_data.db (`eea6bd7`)
- Filter stale and inactive tickers out of the daily scorer (`7531453`)
- Record disk resize and CPU caps (`1408dda`)
- Cap CPU: leave one core free, nice the batch jobs (`c955146`)
- Make backtest.py refuse to score the training period (`ada1f0a`)
- Replace random split with purged chronological split (`8d60189`)
- Add communication conventions: be brief, prefer structure (`26156db`)
- Refresh CLAUDE.md: it had drifted into being misleading (`7aa71b2`)
- Close phase 05: Arena migration complete (`7ff3c9b`)
- Re-size the Strategy Lab compute plan against measured hardware (`e3a8ec0`)

## 2026-09-08 (26 commits)

- Record the full-depth market database (`e4d8274`)
- Record survivorship bias as an open decision with a research path (`e6211bd`)
- Fix feature staleness check for deepened history; add period fallback (`de89bb1`)
- Tag every listing by security type; widen universe to all instruments (`a1966b8`)
- Record the completed feature build (`4033e77`)
- Exclude feature-ineligible tickers from the outstanding count (`0494cba`)
- Add build_features.py: compute the 20 indicators for all history (`04f7f36`)
- Handle Yahoo rate limiting adaptively (`3054407`)
- Record phase 06 results: 20 years of prices loaded (`5574a7a`)
- Reject physically impossible price bars at write time (`70f5691`)
- Document the historical data store and backfill workflow (`6e335b5`)
- Add backfill.py: resumable historical loader (`5343fb2`)
- Record symbol registry and keep dated directory snapshots (`34019c5`)
- Add storage.py: the market-data database layer (`384f9ee`)
- Add all_us universe source from the NASDAQ Trader symbol directory (`fa57ca0`)
- Update roadmap: phase 03 complete, phase 04 to 85% (`3e82e4a`)
- Record smoke test results: all stages pass, backtest number is invalid (`26cefbe`)
- Rename project to Stockbot2000 (`b4ce029`)
- Make commit-on-every-change the project rule (`3427e52`)
- Add four gaps found while reviewing the codebase (`1502ece`)
- Update roadmap: Arena migration in progress, phase 03 near done (`46bf92d`)
- Document account cap and stop-loss limitation in README (`d231188`)
- Record trading mandate, order constraints and migration state in CLAUDE.md (`095e66c`)
- Fix stage counter and absolute path in run_pipeline.sh (`46a82f3`)
- Remove pandas-ta from requirements.txt (`0e5b68a`)
- Initial import: stockpicker2000 as transferred to the Arena VM (`86b7e58`)
